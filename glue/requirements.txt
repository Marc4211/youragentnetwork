fastapi==0.115.6
uvicorn[standard]==0.32.1
openai==1.58.1
httpx==0.28.1
python-multipart==0.0.20
# Pure-Python PDF text extraction (no system deps; installs clean on slim).
# Used at upload ingest to write a readable .txt sidecar next to a PDF, so an
# agent's file-read tool can read it. Text-layer PDFs only; OCR is a later add.
pypdf==5.1.0
# A2A (Agent2Agent) protocol SDK. Used by a2a_client.py to call external,
# A2A-compliant agents so they can participate in channels. Pinned: the SDK is
# young and its API shifted between 0.x and 1.x. See docs/a2a-integration-plan.md.
a2a-sdk==1.1.0
